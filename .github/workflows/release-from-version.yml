name: Release from package version

# Creates a GitHub tag + release when package.json version has no matching tag yet.
# Restricted to main so forks/PRs cannot mint releases.
on:
  push:
    branches: [main]
    paths:
      - "package.json"
      - ".github/workflows/release-from-version.yml"

permissions:
  contents: write
  actions: write

jobs:
  release:
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'
    runs-on: ubuntu-latest
    outputs:
      created: ${{ steps.tag.outputs.created }}
      tag: ${{ steps.version.outputs.tag }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Read package version
        id: version
        run: |
          set -euo pipefail
          VERSION="$(node -p "require('./package.json').version")"
          if [[ ! "$VERSION" =~ ^[0-9]+\.[0-9]+\.[0-9]+([.-][0-9A-Za-z.-]+)?$ ]]; then
            echo "Invalid package.json version: $VERSION"
            exit 1
          fi
          echo "version=$VERSION" >> "$GITHUB_OUTPUT"
          echo "tag=v$VERSION" >> "$GITHUB_OUTPUT"

      - name: Create tag and GitHub release if missing
        id: tag
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          set -euo pipefail
          TAG="${{ steps.version.outputs.tag }}"
          VERSION="${{ steps.version.outputs.version }}"

          if git ls-remote --tags origin "refs/tags/${TAG}" | grep -q "refs/tags/${TAG}$"; then
            echo "Tag ${TAG} already exists — nothing to do."
            echo "created=false" >> "$GITHUB_OUTPUT"
            exit 0
          fi

          echo "Creating tag and release ${TAG}"
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git tag -a "$TAG" -m "Release ${TAG}"
          git push origin "$TAG"

          gh release create "$TAG" \
            --title "setly ${VERSION}" \
            --notes "Release **${VERSION}** from \`package.json\`.

          Published automatically when this version had no existing GitHub tag." \
            --verify-tag

          echo "created=true" >> "$GITHUB_OUTPUT"

      # GITHUB_TOKEN-created releases do not trigger other workflows; dispatch publish explicitly.
      - name: Trigger npm publish workflow
        if: steps.tag.outputs.created == 'true'
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          set -euo pipefail
          gh workflow run "Publish to npm" --ref main -f tag="${{ steps.version.outputs.tag }}"
